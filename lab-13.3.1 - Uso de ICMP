# 🧪 Lab 13.3.1 – Packet Tracer: Usar ICMP para Testar e Corrigir a Conectividade de Rede

Este laboratório tem como objetivo utilizar o protocolo ICMP (Internet Control Message Protocol) para diagnosticar e corrigir problemas de conectividade em uma rede IPv4 e IPv6 simulada no Cisco Packet Tracer.

## 🎯 Objetivos do Laboratório

- Compreender o funcionamento do protocolo ICMP
- Utilizar comandos como `ping` e `traceroute` para testar conectividade
- Identificar falhas de rede causadas por configurações incorretas
- Corrigir problemas de endereçamento, gateway e interfaces inativas
- Validar a comunicação entre dispositivos após ajustes

## 🛠️ Etapas Realizadas

### Parte 1: Testes de Conectividade com ICMP

- Execução de `ping` entre PCs e roteadores
- Testes com `traceroute` para mapear o caminho dos pacotes
- Verificação de respostas ICMP para identificar falhas ou sucesso

### Parte 2: Diagnóstico de Problemas

- Verificação de endereços IP e gateways com `ipconfig` e `show ip interface brief`
- Identificação de interfaces desativadas ou mal configuradas
- Análise de tabelas de roteamento com `show ip route` e `show ipv6 route`
- Verificação de vizinhança IPv6 com `show ipv6 neighbors`

### Parte 3: Correção de Configurações

- Ativação de interfaces com `no shutdown`
- Ajuste de endereços IP, máscaras e gateways padrão
- Salvamento das configurações com `copy running-config startup-config`

### Parte 4: Validação Final

- Repetição dos testes de `ping` e `traceroute`
- Confirmação da conectividade ponta a ponta
- Discussão sobre a importância do ICMP no diagnóstico de redes

## 🧰 Ferramentas Utilizadas

- Cisco Packet Tracer
- Terminal CLI (PCs, switches e roteadores)
- Comandos: `ping`, `traceroute`, `ipconfig`, `show ip interface`, `show ipv6 interface`, `show ip route`, `show ipv6 route`, `show ipv6 neighbors`

## 📎 Arquivos

- `lab-13.3.1.pkt` – Arquivo do Packet Tracer com a topologia configurada
- `README.md` – Este documento com a descrição do laboratório

> 💡 *Este laboratório é essencial para entender como o protocolo ICMP pode ser usado para diagnosticar e corrigir problemas de conectividade em redes IPv4 e IPv6.*
